# Welcome to Sonic Pi v2.11.1
use_synth :piano
st_notes = [ :c2, :e2, :g2, :b2, :c3, :b2, :g2, :e2]
index = 0
loop do
  index = 0
  8.times do
    play st_notes[index]
    sleep 0.2
    index = index + 1
  end
end
